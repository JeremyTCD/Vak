@model string

@{
    ViewData["Title"] = StringOptionsAccessor.Value.ResetPasswordConfirmationView_Title;
}

<h2>@StringOptionsAccessor.Value.ResetPasswordConfirmationView_Title.</h2>
<div>
    logo
</div>
<div>
    Password reset for @Model.
</div>
<div>
    <a asp-controller="@nameof(AccountController).Replace("Controller", "")" asp-action="@nameof(AccountController.LogIn)">Log in</a>
</div>